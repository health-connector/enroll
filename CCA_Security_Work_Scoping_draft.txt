# CCA Security Work Scoping

Analysis of all Feature Flags (High) (all clients)
  - outcome: produce document that outlines all feature flags with the following information:
    - environment variable name used to enable/disable the feature flag (if applicable)
    - any settings plus associated environment variables (if applicable)
    - current setting in Production (enabled or disabled); note: must be based on k8s env configmap if env variable is used to set the value
    - brief description of what the feature flag is for (functionality and high level code path)
    - when it was added
    - if the feature flag is intended to be long-lived (i.e. it controls some client config) or if it is intended to be short-lived (i.e. it was addded to support trunk based development)
    - a recommendation on whether to remove the feature flag or keep it
    - open questions that must be answered to make a high confidence recommendation

CK Editor (Medium) (all clients)
  - confirm alignment on agreed upon approach for upgrading/replacing CK Editor internally
  - communicate to client impacts of the approach (e.g., licensing fee, UI may look different for notice templates, etc.)
  - estimate lift and time to complete the agreed upon approach

Datatables (Medium) (MA only; eventually all clients)
  - implement and test POC for replacing datatables in a deployed env with prod-like data
  - communicate to client any impacts of replacing datatables (.e.g. the tables may look different in the UI, etc.)

Make sure all our security GHAs are in all the repos (High)
CURRENTLY ONLY VIABLE IN IC REPOS; DC AND MA REQUIRE CLIENT COMMUNICATION AND APPROVAL
  - 4 code scanning tools:
    - Bearer
    - Brakeman
    - CodeQL
    - Grype
  - for ME, we have full control of this. this means confirming/adding all the tools to the following repos:
    - enroll
    - medicaid_gateway
    - fdsh_gateway
    - edi_gateway
    - polypress
    - aca_entities
    - resource_registry
    - event_source
    - fti
    - console
    - crm_gateway
    - identity_provider_gateway
    - medicaid_eligibility
    - cartafact
    - glue?

Establish Security Baseline post ROR.v.8.1/3.2 upgrade, ongoing maintenance strategy proposal, and remediation (High) (all clients)
  - for all repos, generate a report of all the security vulnerabilities in the codebase currently flagged by the following tools (where applicable):
    - Bearer
    - Brakeman
    - CodeQL
    - Grype
  - break down the findings by severity and vulnerability type
  - remediate findings in order of severity starting with critical vulnerabilities
  - goal is to eliminate all findings, but MVP is critical and high severity findings

Dead code cleanup (High) (all clients)
  - outcome (phase 1): generate a report of all the dead code in the codebase, including:
    - description of the functionality
    - justification for why the code is dead (e.g. it was moved to a different namespace, it was replaced by a new feature, it was abandoned during development, etc.)
    - locations in the codebase and code paths
    - recommendation with confidence level of how to remove the code
    - open questions that must be answered to make clean up high confidence
    - notes on what should be done for regression testing to make sure removal did not break anything
  - outcome (phase 2): determine which clean up efforts are safest and most feasible given project constraints and then remove the code
    

Data anonymization of db dumps (all clients)
  - NEED TO PRESENT THIS SOLUTION TO LEAD ENGINEERS BEFORE PROCEEDING
  - outcome: produce an anonymized data dump that developers and QAs can use locally for testing and feature development, thus reducing reliance on remote environments.


Set up local k8s cluster and test data dump for QA (High) (MA and DC only)
  - outcome: QAs can run the app locally via k8s and pre-built images along with a dump of mock data, thereby eliminating the need for QA testing against remote environments

Remove aca_entities dependency from MA Enroll (High/Urgent) (MA only)
  - outcome: enroll app does not include aca_entities gem; Premium Value Product (PVP) functionality is unaffected and works exactly as it did before

Removal of “Mongoid::Attributes::Dynamic” (Medium) (all clients)
  - outcome: no models in the codebase use the Mongoid::Attributes::Dynamic module

Clean up all our unwanted/unneeded indexes. This is slowing down the writes to the DB, performance impact. (Low) (all clients)
  - outcome: no unwanted/unneeded indexes in the codebase

Clean up all the legacy acapi listeners that are not in use today. (Low) (all clients)
  - outcome: no legacy acapi listeners in the codebase

Upgrade debian and node versions (High) (all clients)
  - outcome: Debian 13 (trixie) and Node 24 used to build the images
  - note: this may be handled by devops team